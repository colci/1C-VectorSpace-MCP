# Контекст и Память Проекта: 1C RAG & MCP Server

Этот файл содержит ключевую информацию о проекте, текущем состоянии инфраструктуры и выполненных шагах, чтобы любой ИИ-ассистент в новых сессиях мог мгновенно восстановить контекст работы.

---

## 1. Цель проекта
Создать локальную систему RAG (Retrieval-Augmented Generation) для конфигурации 1С:Управление Нашей Фирмой (УНФ) на основе векторной базы данных Qdrant и предоставить доступ к ней для LLM через MCP-сервер (Model Context Protocol).

---

## 2. Инфраструктура и окружение

*   **Рабочая папка проекта:** `d:\work\Test2`
*   **Папка выгрузки конфигурации 1С в файлы (XML + BSL):** `D:\Export\UNF`
*   **Путь к файловой базе данных 1С:** `D:\Base\УНФ`
*   **Векторная БД Qdrant:**
    *   Запущена локально в Docker-контейнере с именем `qdrant-local`.
    *   Данные сохраняются в именованный Docker Volume `qdrant_storage`.
    *   **HTTP API / Web Dashboard:** `http://localhost:6333` (успешно протестировано)
    *   **gRPC API:** `http://localhost:6334`

---

## 3. Созданные файлы в проекте (`d:\work\Test2`)

1.  **[export_config.ps1](file:///d:/work/Test2/export_config.ps1)**
    *   Скрипт на PowerShell для автоматической выгрузки конфигурации 1С с помощью утилиты `ibcmd`.
    *   Использует флаг `--sync` для инкрементального (быстрого) обновления файлов.
    *   Автоматически определяет последнюю установленную версию платформы 1С в папке `C:\Program Files\1cv8`.
2.  **[implementation_plan.md](file:///d:/work/Test2/implementation_plan.md)**
    *   Подробный технический план реализации: стратегия чанкинга кода BSL (по методам) и метаданных XML (структурированные карточки объектов), схема коллекции в Qdrant и описание инструментов MCP-сервера.

---

## 4. Текущий статус и прогресс

*   [x] Запущен и проверен Qdrant в Docker.
*   [x] Разработан и сохранен PowerShell-скрипт инкрементальной выгрузки 1С.
*   [x] Разработан и согласован план архитектуры RAG + MCP.
*   [x] Создание `requirements.txt` (окружение Python).
*   [x] Разработка индексатора `index_config.py`.
*   [x] Разработка MCP-сервера `mcp_server.py`.

---

## 5. Важные проектные решения и договоренности
*   **Поэтапный подход к графовой базе данных (GraphRAG):** 
    *   **Этап 1 (Текущий):** Полноценная графовая СУБД (например, Neo4j) не используется, чтобы избежать усложнения инфраструктуры. Все усилия направлены на запуск чистого векторного RAG на базе Qdrant, который покроет 80% задач разработки.
    *   **Этап 2 (Graph Light):** Будет реализован легкий анализ зависимостей. Скрипт индексации при парсинге XML соберет структуру связей в JSON-файл, а MCP-сервер получит инструмент `get_dependencies` для выдачи связей ИИ-модели.
    *   **Этап 3 (Полноценный граф):** При необходимости глубокого анализа цепочек вызовов (Call Graph) на уровне AST-дерева языка BSL будет развернута графовая база данных (например, Memgraph или FalkorDB) с поддержкой Cypher.

*   **Чанкинг кода:** Строго по границам процедур и функций (`Процедура ... КонецПроцедуры`), с добавлением заголовочного контекста (имя модуля, тип модуля, экспортность).

*   **Гибкая настройка эмбеддингов (Перспективный этап):** Планируется вынести все параметры внешних моделей и API (OpenAI, Ollama, Cohere, Hugging Face, Yandex) в файл конфигурации `.env` и создать единую обертку (интерфейс) для переключения между провайдерами эмбеддингов без правки кода.

