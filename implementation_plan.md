# План реализации и развития проекта 1C-VectorSpace-MCP

Этот документ объединяет все этапы проектирования системы: текущие задачи по оптимизации и архив решений базовой архитектуры.

---

# [ТЕКУЩИЙ ЭТАП] Визуализация прогресса и оптимизация отправки векторов (Этап 3)

Этот этап решает проблему непрозрачности процесса индексации при загрузке данных в Qdrant. Пользователь не видит реального прогресса на долгих операциях (таких как вычисление эмбеддингов локально или отправка запросов по API).

## Proposed Changes

### [index_config.py](file:///d:/work/Test2/index_config.py)
#### [MODIFY] [index_config.py](file:///d:/work/Test2/index_config.py)
1. **Быстрая детекция изменений**:
   - Предварительно сканируем все файлы и сравниваем их с кэшем без отображения прогресс-бара (занимает < 1 сек).
   - Если изменений нет, скрипт мгновенно выводит `"Все файлы актуальны. Изменений не обнаружено."` и завершается.
   - Если изменения есть, выводится точное количество измененных файлов, и создается прогресс-бар `tqdm`, отслеживающий именно измененные файлы.
2. **Предотвращение поломки прогресс-бара**:
   - Передаем ссылку на текущий `pbar` во вспомогательные функции (`flush_batch`, `save_cache`).
   - Используем `pbar.write()` вместо обычного `print()` во время активного прогресс-бара, чтобы вывод сообщений не ломал строку прогресса в консоли (особенно в PowerShell).
3. **Отображение прогресса генерации эмбеддингов**:
   - **FastEmbed**: Генератор `encoder.embed(docs)` оборачивается во второй (вложенный) `tqdm` с параметром `leave=False`, чтобы пользователь видел реальный прогресс генерации векторов для текущего батча на процессоре.
   - **OpenAI**: Оптимизируем вызов API. Вместо одиночных вызовов `get_embedding(doc)` в цикле (которые вызывают 100 отдельных HTTP-запросов и долго висят без прогресса), отправляем весь батч одним запросом `openai_client.embeddings.create(input=docs)`. Это ускорит OpenAI-индексацию в десятки раз и сделает ее мгновенной. Добавляем информативный вывод о запросе.

## Verification Plan

### Automated Tests
- Запуск индексации изменений через `run_index.ps1` или напрямую `index_config.py` и проверка корректного отображения прогресс-бара.
- Проверка поведения скрипта при отсутствии изменений (должен завершаться моментально с понятным логом).

### Manual Verification
- Визуальный контроль прогресс-баров в консоли PowerShell: основной прогресс-бар по файлам должен идти плавно, а вывод логов о батчах должен появляться над ним, не дублируя строки.

---

# [ВЫПОЛНЕНО] Оптимизация памяти и предотвращение зависаний (Этап 2)

Этот этап описывает изменения для повышения стабильности работы индексатора `index_config.py`, ограничения использования оперативной памяти (RAM) до уровня не более 70% и оптимизации работы библиотеки FastEmbed (ONNX Runtime) для предотвращения зависаний операционной системы.

## User Review Required

> [!WARNING]
> Для мониторинга системной памяти в проект добавляется зависимость `psutil`. Она будет автоматически установлена при обновлении окружения.
> Будут изменены настройки по умолчанию (размер батча уменьшен с 1000 до 100, количество потоков FastEmbed ограничено до 2).

## Proposed Changes

### [requirements.txt](file:///d:/work/Test2/requirements.txt)
#### [MODIFY] [requirements.txt](file:///d:/work/Test2/requirements.txt)
- Добавление библиотеки `psutil` для отслеживания загрузки оперативной памяти.

### [index_config.py](file:///d:/work/Test2/index_config.py)
#### [MODIFY] [index_config.py](file:///d:/work/Test2/index_config.py)
- Импорт `psutil` и реализация защитного механизма `check_memory_limit()`.
- Уменьшение размера батча по умолчанию в кодовой базе (с 1000 до 100).
- Изменение логики вычисления потоков по умолчанию: ограничение до `min(4, CPU_COUNT // 2)` или жестко до `2` по умолчанию для снижения пиковой нагрузки.
- Постраничная передача в FastEmbed через параметр `batch_size=32` при генерации векторов (вместо дефолтных 256), чтобы снизить пиковое потребление памяти ONNX Runtime.
- Интеграция вызовов `check_memory_limit` перед обработкой батчей и генерацией эмбеддингов. При превышении 70% памяти скрипт будет вызывать `gc.collect()` и засыпать на 5 секунд. Если по истечении 30 секунд память не опустилась ниже 70%, выводится предупреждение, и процесс продолжается. При критическом превышении (>85%) процесс будет ожидать освобождения памяти во избежание OOM-килла.

### [.env](file:///d:/work/Test2/.env)
#### [MODIFY] [.env](file:///d:/work/Test2/.env)
- Добавление настроек лимитов памяти и потоков:
  - `INDEX_BATCH_SIZE=100` (для снижения объема данных в одном батче)
  - `FASTEMBED_THREADS=2` (для предотвращения 100% загрузки всех ядер CPU)
  - `MAX_RAM_PERCENT=70` (максимальный процент использования RAM)

## Verification Plan

### Automated Tests
- Запуск `pip install -r requirements.txt` для установки `psutil`.
- Тестовый запуск индексации с искусственно заниженным лимитом памяти (например, `MAX_RAM_PERCENT=40`) для проверки срабатывания защитного механизма throttling (ожидания освобождения памяти).
- Запуск полной индексации тестового набора файлов (например, `RFIDКлиент`) с замером потребления памяти.

---

# [ВЫПОЛНЕНО] Базовая реализация RAG и MCP (Этап 1)

Этот этап описывает базовую логику создания системы семантического поиска и предоставления контекста по структуре и коду конфигурации 1С:УНФ с использованием векторной базы данных Qdrant и протокола Model Context Protocol (MCP).

## 1. Стратегия чанкинга (нарезки) данных 1С

Выгрузка конфигурации 1С состоит из двух основных типов файлов:
1. **XML-файлы описания метаданных** (структура справочников, документов, регистров и т.д.).
2. **BSL-файлы программного кода** (модули объектов, менеджеры, общие модули, модули форм с расширением `.bsl`).

Мы применили **структурный семантический чанкинг**.

### А. Чанкинг BSL-кода (по процедурам и функциям)
Каждый программный модуль (`.bsl` файл) парсится на отдельные методы:
* **Границы чанка:** Начало метода (`Процедура` / `Функция`) и его конец (`КонецПроцедуры` / `КонецФункции`).
* **Метаданные чанка (Payload):**
  * `module_path` (например, `CommonModules.RFIDКлиент`)
  * `module_type` (например, `CommonModule`, `ObjectModule`, `ManagerModule`, `FormModule`)
  * `method_name` (например, `ЗаписатьДанныеВRFID`)
  * `is_export` (экспортный метод или локальный)
  * `file_path` (путь к файлу)
  * `lines` (диапазон строк в исходном файле, например `57-120`)
* **Контекст чанка (Text to Embed):**
  Перед телом функции добавляется заголовочный контекст, чтобы модель эмбеддингов лучше понимала область видимости:
  ```text
  Модуль: ОбщийМодуль.RFIDКлиент
  Метод: ЗаписатьДанныеВRFID (Экспортный)
  Код:
  Процедура ЗаписатьДанныеВRFID(Результат, Параметры) Экспорт
      // [Тело метода...]
  КонецПроцедуры
  ```

### Б. Чанкинг XML-метаданных
Вместо индексации сырого XML, мы генерируем краткое структурированное текстовое описание (карточку объекта) на основе XML:
* **Что извлекаем из XML:** Имя объекта, синоним, список реквизитов (имя, тип, синоним, пояснение), список табличных частей, связанные формы.
* **Пример формируемого текста чанка:**
  ```text
  Справочник: Номенклатура (Catalogs.Номенклатура)
  Синоним: Номенклатура
  Описание: Хранит информацию о товарах, услугах и работах.
  Реквизиты:
    - Артикул (Строка, 25): Код производителя или артикул товара
    - ВидНоменклатуры (СправочникСсылка.КатегорииНоменклатуры): Категория товара
    - СтавкаНДС (СправочникСсылка.СтавкиСчет): Основная ставка НДС
  Табличные части:
    - Характеристики (Характеристика, Штрихкод)
  ```
* **Метаданные чанка (Payload):**
  * `object_type` (`Catalog`, `Document`, `InformationRegister`, и т.д.)
  * `object_name` (`Номенклатура`)
  * `file_path` (путь к XML)

---

## 2. Схема коллекции Qdrant

Мы создали коллекцию `1c_unf_configuration` со следующими параметрами:
* **Размерность вектора:** 1024 для `multilingual-e5-large`.
* **Метрика расстояния:** `Cosine` (косинусное сходство).
* **Payload-индексы** для быстрой фильтрации:
  * Индекс на поле `module_type` (keyword)
  * Индекс на поле `object_type` (keyword)
  * Индекс на поле `method_name` (text/keyword)

---

## 3. Архитектура MCP-сервера

MCP-сервер написан на **Python** (с использованием `fastmcp` SDK) и предоставляет AI-ассистенту инструменты для работы с кодовой передней частью 1С.

### Доступные инструменты (Tools):
1. **`search_code`**: Семантический поиск по коду модулей (BSL) с возможностью фильтрации по типу модуля или имени метода.
2. **`search_metadata`**: Семантический поиск по структуре метаданных (XML) для поиска нужных реквизитов или табличных частей объектов.
3. **`get_file_snippet`**: Чтение оригинального фрагмента кода с диска.

---

## 4. Развитие системы: Графовая база данных (Этап 2 - Перспектива)

После успешного запуска векторного RAG планируется расширение системы до гибридной схемы **GraphRAG** для точного анализа взаимосвязей и зависимостей конфигурации 1С.

### Подход "Graph Light" (Упрощенный граф)
Для минимизации инфраструктурных затрат (без развертывания серверов вроде Neo4j):
1. **Сбор связей при индексации:** Скрипт `index_config.py` при парсинге XML-файлов метаданных будет собирать зависимости (например, связь *"Документ X содержит реквизит типа Справочник Y"* или вызовы общих модулей в BSL-коде).
2. **Интеграция в Qdrant:** Эти связи будут записываться в текстовом виде непосредственно в карточки объектов и BSL-чанки, а также сохраняться в локальный JSON-файл связей проекта.
3. **MCP-инструмент:** В MCP-сервер будет добавлен инструмент `get_dependencies`, позволяющий модели мгновенно получать граф зависимостей для конкретного объекта метаданных.
